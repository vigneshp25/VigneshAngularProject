import { ConjunctionwordspipePipe } from './conjunctionwordspipe.pipe';

describe('ConjunctionwordspipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConjunctionwordspipePipe();
    expect(pipe).toBeTruthy();
  });
});
