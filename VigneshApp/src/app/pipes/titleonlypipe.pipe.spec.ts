import { TitleonlypipePipe } from './titleonlypipe.pipe';

describe('TitleonlypipePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleonlypipePipe();
    expect(pipe).toBeTruthy();
  });
});
