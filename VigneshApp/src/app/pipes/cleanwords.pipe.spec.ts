import { CleanwordsPipe } from './cleanwords.pipe';

describe('CleanwordsPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanwordsPipe();
    expect(pipe).toBeTruthy();
  });
});
